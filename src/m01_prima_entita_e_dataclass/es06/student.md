# Design Esercizio 06: Dominio Magazzino (Articolo Magazzino)

## 1. Modello ER (Database su Disco)

<!-- 
Disegna da zero il diagramma Mermaid erDiagram per l'entità ARTICOLO_MAGAZZINO.
Campi: codice_sku (PK string), nome (string), prezzo_unitario (float), 
quantita_disponibile (int), scorta_minima (int).
-->

```mermaid
erDiagram
    %% Scrivi qui la definizione dell'entità ARTICOLO_MAGAZZINO
```

---

## 2. Diagramma delle Classi UML (RAM)

<!-- 
Disegna da zero il diagramma Mermaid classDiagram per la classe ArticoloMagazzino.
Includi attributi tipizzati e metodi: scarica, rifornisci, sotto_scorta, valore_inventario.
-->

```mermaid
classDiagram
    %% Scrivi qui la definizione della classe ArticoloMagazzino
```
